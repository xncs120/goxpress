services:
  app:
    build: .
    ports:
      - "${APP_PORT:-8080}:8080"
    environment:
      - APP_NAME
      - APP_ENV
      - APP_DEBUG
      - APP_HOST
      - APP_PORT
      - JWT_SECRET_KEY
      - JWT_EXPIRE_MINUTES
      - DB_URL

  # for more services

volumes:
  app_data: